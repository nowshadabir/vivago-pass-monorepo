# Vivago Pass - Mobile Client

This directory is designated for the future mobile client built using Flutter (Dart).

It will implement the identical Zero-Knowledge cryptographic pipelines defined in `@vivago-pass/ts-crypto`.
